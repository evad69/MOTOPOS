import { PagePlaceholder } from "@/components/PagePlaceholder";

/** Renders a placeholder sale history page until history features are implemented. */
export default function HistoryPage() {
  return <PagePlaceholder phaseLabel="Phase 8" title="Sale History" />;
}
