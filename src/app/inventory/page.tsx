import { PagePlaceholder } from "@/components/PagePlaceholder";

/** Renders a placeholder inventory page until inventory features are implemented. */
export default function InventoryPage() {
  return <PagePlaceholder phaseLabel="Phase 5" title="Inventory" />;
}
